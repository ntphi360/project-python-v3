# KHAI BÁO SỬ DỤNG AI

# PROMPTING FRONTEND

### Mục đích sử dụng AI
AI được sử dụng để hỗ trợ tìm hiểu nghiệp vụ thủ tục hành chính, xây dựng dữ liệu mẫu, thiết kế giao diện React, trực quan hóa dữ liệu bằng Recharts và hỗ trợ kết nối giữa frontend với backend.

### Các prompt đã sử dụng
- "Cách sử dụng Recharts trong React?"
- "Phân tích luồng đi TTHC."
- "Hãy mock data về thông tin hồ sơ TTHC, phòng ban, lĩnh vực, người xử lý."
- "Hãy cho tôi biết về số ngày phải giải quyết một TTHC trước khi đến hẹn trả."
- "Hãy cho tôi biết quy định về thời hạn giải quyết hồ sơ của công chức khi trả kết quả trước hạn?"
- "Công chức có thể trả kết quả trước thời hạn hẹn không?"
- "Có phải tùy từng loại thủ tục hành chính mà thời gian giải quyết hồ sơ và hẹn trả sẽ khác nhau không?"
- "Nếu hẹn trong ngày thì nên thiết lập thời gian như thế nào để người xử lý thuận tiện nhất?"
- "Cách setting CORS trong Flask."

### Kết quả áp dụng
Các gợi ý từ AI được tham khảo để xây dựng dashboard, biểu đồ thống kê, giao diện quản lý hồ sơ, dữ liệu mẫu và hỗ trợ kết nối giữa frontend React với backend Flask.

---

# PROMPTING BACKEND

### Mục đích sử dụng AI
AI được sử dụng để hỗ trợ tổ chức Flask backend, xây dựng API, thiết kế quan hệ cơ sở dữ liệu, xử lý dữ liệu CSV/Excel, xử lý dữ liệu khuyết, chuẩn hóa dữ liệu, lưu dữ liệu vào SQL Server, authentication/authorization và gửi email.

### Các prompt đã sử dụng
- "Cấu trúc và cách tổ chức project của Flask."
- "Các thư viện built-in và thư viện hỗ trợ khác của Flask."
- "Cách tổ chức router trong Flask."
- "Làm sao để ràng buộc relationship giữa các table?"
- "Cách viết model bằng SQLAlchemy trong Flask."
- "Cách khai báo column, kiểu dữ liệu, primary key, foreign key và relationship trong SQLAlchemy Model."
- "Cách sử dụng Alembic/Flask-Migrate để tạo và cập nhật schema database."
- "Cách tạo migration sau khi thay đổi SQLAlchemy Model."
- "Cách import file CSV/Excel trong Flask."
- "Ngoài Pandas còn có những thư viện nào để xử lý CSV/Excel?"
- "Làm sao để viết test case CRUD trong Python Flask?"
- "Cách format mã hồ sơ tự động."
- "Cách lấy session trong Flask."
- "Cách sử dụng authentication/authorization trong Flask."
- "Flask có thư viện để gửi mail không?"
- "Config Flask-Mail với Gmail có cần tạo App Password không?"
- "Cách xử lý dữ liệu bị khuyết/null trong file CSV/Excel trước khi import."
- "Nên xử lý các giá trị thiếu bằng cách xóa dòng, điền giá trị mặc định hay giữ NULL?"
- "Cách chuẩn hóa dữ liệu trước khi lưu vào database."
- "Cách đọc dữ liệu từ file CSV/Excel bằng Pandas và lưu vào SQL Server."
- "Cách map dữ liệu từ DataFrame sang các model SQLAlchemy."
- "Cách lưu nhiều bản ghi vào database trong Flask."
- "Cách kiểm tra dữ liệu trùng trước khi insert vào database."
- "Cách xử lý transaction, commit và rollback khi import dữ liệu."

### Kết quả áp dụng
Các gợi ý được tham khảo trong quá trình xây dựng REST API, tổ chức Blueprint, xử lý migration, import CSV/Excel, xử lý dữ liệu khuyết, chuẩn hóa dữ liệu, ánh xạ dữ liệu vào model SQLAlchemy, lưu dữ liệu vào SQL Server, xác thực người dùng và chức năng gửi email nhắc nhở.

---

# AI SKILLS USED

- AI Coding hỗ trợ gợi ý code, chỉnh sửa code, debug và tối ưu một số chức năng.
- Phân tích nghiệp vụ thủ tục hành chính.
- Thiết kế giao diện React.
- Trực quan hóa dữ liệu bằng Recharts.
- Thiết kế cấu trúc Flask Backend.
- Thiết kế REST API.
- Thiết kế và ràng buộc cơ sở dữ liệu.
- Authentication và Authorization.
- Import dữ liệu CSV/Excel.
- Xử lý dữ liệu khuyết.
- Chuẩn hóa dữ liệu trước khi lưu.
- Ánh xạ dữ liệu vào SQLAlchemy Model.
- Database Migration.
- Viết test case.
- Cấu hình CORS.
- Cấu hình gửi email bằng Flask-Mail.
- Debug và xử lý lỗi.
